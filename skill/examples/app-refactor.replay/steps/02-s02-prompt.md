我有一个 Express + Sequelize 的 REST API 项目，想迁移到 Hono + Drizzle ORM。

请先帮我分析现有代码结构，然后给出迁移方案：

1. 读一下 `src/` 下所有文件，梳理路由、中间件、数据模型
2. 列出所有 API 端点和中间件层级
3. 找出和 Express 强耦合的部分（哪些需要改，哪些可以直接复用）
4. 输出一份迁移方案，包括优先级和可能的风险点

注意：前端不动，API 接口签名必须保持一致。
